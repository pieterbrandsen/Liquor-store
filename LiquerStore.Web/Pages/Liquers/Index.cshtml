@page
@model LiquerStore.Web.Pages.Liquers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table id="table" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.WhiskyModel[0].Name)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.WhiskyModel[0].Age)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WhiskyModel[0].ProductionArea)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WhiskyModel[0].AlcoholPercentage)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WhiskyModel[0].Kind)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WhiskyModel[0].LabelPath)
                </th>
                <th></th>*@
        </tr>
    </thead>
    @*<tbody>
        @foreach (var item in Model.WhiskyModel) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Age)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ProductionArea)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AlcoholPercentage)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Kind)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LabelPath)
                    </td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
        }
            </tbody>*@
</table>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#table').DataTable({
                data: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.WhiskyModel)),
                columns: [
                    { "data": "Name", "name": "Name" },
                ]
            });
        });

        console.log(@Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model.WhiskyModel)))
    </script>
}